services:
  - type: web
    name: eraapi
    env: docker
    dockerfilePath: Dockerfile
    branch: main
    region: oregon
    plan: free
    # Configure the database credentials in the Render dashboard or secret manager.
    # Do not commit secrets to source control.